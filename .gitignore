# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
.claude/

# Lockfile omitted: generated locally against a China npm mirror, so its
# integrity hashes don't match official npm. CI runs `npm install` to
# resolve fresh from registry.npmjs.org. Do not commit package-lock.json.
package-lock.json
